



const objects52 = function () {

    // function compareNumber(a, b) {
    //     if (a < b) return 1;
    //     if (a > b) return -1;
    //     if (a == b) return 0;
    // }

   let arr = ["алерт", "ретерн", "функция"];

    alert (arr.sort());

};